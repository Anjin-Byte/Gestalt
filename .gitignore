node_modules
.DS_Store
pnpm-lock.yaml

# ── apps/gestalt (active app) ──────────────────────────────────────────────
apps/gestalt/dist
apps/gestalt/.vite
apps/gestalt/playwright-report
apps/gestalt/test-results
# wasm-pack output dirs (generated by pnpm build:wasm)
apps/gestalt/src/wasm/wasm_obj_loader/
apps/gestalt/src/wasm/wasm_webgpu_demo/
apps/gestalt/src/wasm/wasm_voxelizer/
apps/gestalt/src/wasm/wasm_greedy_mesher/

# ── legacy/apps/web ────────────────────────────────────────────────────────
# wasm output dirs in legacy have their own per-directory .gitignore (*).
# dist is not tracked for the legacy app.
legacy/apps/web/dist
legacy/apps/web/.vite
legacy/apps/web/playwright-report
legacy/apps/web/test-results

# ── Rust ───────────────────────────────────────────────────────────────────
target
**/target

# -- Ref --------────────────────────────────────────────────────────────────
reference/repos/*