WASM build outputs go here.

Build the example modules from the repo root:

  pnpm build:wasm

This will overwrite the placeholder outputs in `wasm_example`, `wasm_points`, `wasm_obj_loader`, and `wasm_webgpu_demo`.
