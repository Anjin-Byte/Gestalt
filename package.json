{
  "name": "gestalt",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "dev": "pnpm -C apps/gestalt dev",
    "dev:legacy": "pnpm -C legacy/apps/web dev",
    "build": "pnpm -C apps/gestalt build",
    "build:legacy": "pnpm -C legacy/apps/web build",
    "build:wasm": "cd crates/wasm_obj_loader && wasm-pack build --target web --out-dir ../../apps/gestalt/src/wasm/wasm_obj_loader && cd ../wasm_webgpu_demo && wasm-pack build --target web --out-dir ../../apps/gestalt/src/wasm/wasm_webgpu_demo && cd ../wasm_voxelizer && wasm-pack build --target web --out-dir ../../apps/gestalt/src/wasm/wasm_voxelizer && cd ../wasm_greedy_mesher && wasm-pack build --target web --out-dir ../../apps/gestalt/src/wasm/wasm_greedy_mesher",
    "preview": "pnpm -C apps/gestalt preview",
    "test": "pnpm -C apps/gestalt test",
    "test:e2e": "pnpm -C apps/gestalt test:e2e"
  }
}
